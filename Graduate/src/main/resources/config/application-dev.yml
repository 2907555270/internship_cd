# 开发环境
spring:
  #  redis
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379


server:
  port: 8080
  servlet:
    context-path: /

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl